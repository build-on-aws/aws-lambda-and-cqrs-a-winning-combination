{
  "name": "library",
  "version": "1.0.0",
  "description": "This is a small example of CRUD to CQRS refactoring that will be deployed in AWS Lambda environment.",
  "main": "src/handler.js",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@aws-lambda-powertools/logger": "~1.15.0",
    "@aws-lambda-powertools/metrics": "~1.15.0",
    "@aws-lambda-powertools/tracer": "~1.15.0",
    "@middy/core": "~4.7.0",
    "esbuild": "~0.14.14"
  },
  "scripts": {
    "lint": "npx eslint .",
    "build": "npx tsc",
    "test": "jest --runInBand",
    "development": "npm run lint && npm run build && npm run test"
  }
}
