{
  "name": "library-system-common-layer",
  "description": "Example AWS Lambda layer with Library System common implementation.",
  "main": "index.ts",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "~20.8.10",
    "typescript": "~5.2.2",
    "moment": "~2.29.4"
  },
  "scripts": {
    "lint": "npx eslint .",
    "build": "node_modules/typescript/bin/tsc",
    "test": "jest --runInBand --silent",
    "development": "npm run build && npm run lint && npm run test"
  }
}
